package org.anime_game_servers.game_data_models.loader

/**
 * TODO some workaround for some compile problems
 */
enum class FolderType {
    BINOUT,
    EXCEL,
    EXCEL_DEPRECATED,
    PATCH,
    OTHER
}